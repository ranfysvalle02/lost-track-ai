# Runtime dependencies for demo.py (and the model-free test_demo.py).
# Sensible version floors, not pins — the demo uses stable, widely-available APIs.
# NOTE: smongo (embedded MongoDB) requires Python >=3.11 and ships a compiled Rust
# extension; it pulls in pymongo as a transitive dependency.
torch>=2.2
transformers>=4.44
scikit-learn>=1.3
numpy>=1.24
smongo>=1.2
pymongo>=4.6
# Optional: only needed for the contrasting `compare` model stabilityai/stablelm-2-1_6b-chat,
# whose remote-code (Arcade100k) tokenizer uses tiktoken. The core demo does not require it.
# tiktoken>=0.5
